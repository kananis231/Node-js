<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
<style>
    
body {
    font-family: 'Arial', sans-serif;
    margin: 0;
    padding: 0;
    background-color: #f4f4f9;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    min-height: 100vh;
}

h1 {
    color: #333;
    font-size: 2.5em;
    margin-bottom: 20px;
}

.container {
    background-color: #fff;
    padding: 30px;
    border-radius: 10px;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    width: 350px;
    display: flex;
    flex-direction: column;
    align-items: center;
}
form {
    width: 100%;
    display: flex;
    flex-direction: column;
    gap: 15px;
    align-items: center;
}

input[type="file"],
input[type="text"],
input[type="number"],
input[type="submit"] {
    padding: 10px;
    font-size: 1em;
    border-radius: 5px;
    border: 1px solid #ccc;
    width: 100%;
}

input[type="file"] {
    font-size: 0.9em;
}

input[type="submit"] {
    background-color: #ff5c5c;
    color: white;
    border: none;
    cursor: pointer;
    transition: background-color 0.3s;
}

input[type="submit"]:hover {
    background-color: #ff2c2c;
}

input::placeholder {
    color: #999;
    font-size: 0.9em;
}


img {
    margin-bottom: 20px;
    border-radius: 10px;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.2);
}


</style>
  
</head>
<body>
    <div class="container" >
        <h1>Update Data</h1>
        <form action="/updatemovie?id=<%=editschema.id%>" method="post" enctype="multipart/form-data" >
            <img src="<%=editschema.image%>" width="30%" alt="">
            <input type="text" name="name" placeholder="Edit movie name" value="<%=editschema.name%>">
            <input type="text" name="movietype" placeholder="Edit movie type" value="<%=editschema.movietype%>">
            <input type="number" name="rating" placeholder="Edit rating" value="<%=editschema.rating%>" >

            <input type="file" name="image">
            
            <input type="submit" value="Update">
        </form>
    </div>
</body>
</html>
